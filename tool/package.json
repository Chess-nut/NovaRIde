{
  "name": "novaride-tool",
  "private": true,
  "type": "module",
  "description": "Operator scripts for the NovaRide Firebase project. They authenticate with a service-account key that lives OUTSIDE the repository — see set_admin_claims.mjs.",
  "scripts": {
    "claims": "node set_admin_claims.mjs"
  },
  "dependencies": {
    "firebase-admin": "^13.0.0"
  }
}
